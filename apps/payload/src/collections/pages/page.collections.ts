import { pageCollection } from '@payload/collections/pages/page.collection'
import { postCollection } from '@payload/collections/pages/post.collection'

export const pageCollections = [
  pageCollection,
  postCollection,
]
