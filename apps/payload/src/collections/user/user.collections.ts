import { addressCollection } from '@payload/collections/user/address.collection'
import { userCollection } from '@payload/collections/user/user.collection'

export const userCollections = [
  userCollection,
  addressCollection,
]
