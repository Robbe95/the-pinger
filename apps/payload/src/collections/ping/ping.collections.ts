import { pingCollection } from '@payload/collections/ping/ping.collection'
import { routeGroupCollection } from '@payload/collections/ping/routeGroup.collection'

export const pingCollections = [
  routeGroupCollection,
  pingCollection,
]
